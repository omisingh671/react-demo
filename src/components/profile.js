import React from 'react';

const Profile = (props) => {
    console.log(props);
    return (
        <div className="Profile">
            <p>Profile</p>
        </div>
    );
}

export default Profile;